# Excel 样式规范

- 不使用大面积顶部色条
- 不使用异常合并空白区
- 表头统一深色底白字
- 正文白底、浅边框、自动换行
- 参考图片列固定宽度
- 图片行固定行高
- 文案列必须启用换行
- 不新增本地路径、图片路径或附件路径列
- 尺寸/重量参数必须有单位换算说明：`cm × 0.3937 = in`，`kg × 2.20462 = lb`；美工执行列优先展示英寸/磅 in/lb
- 所有 Sheet 使用一致的列顺序和样式
